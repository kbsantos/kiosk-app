# K18 Bluetooth Printer Setup — XP-58H

## Current scope

K18 first-stage Bluetooth setup adds paired-printer discovery, Bluetooth status, printer selection, connection checking, and persistence for the XP-58H.

Direct ESC/POS receipt generation/printing is intentionally the next K18 step. The existing PDF/system-printer path remains unchanged for now.

## Device setup

1. Turn on the XP-58H.
2. Pair the XP-58H with the kiosk device using the operating system's Bluetooth settings.
3. Open **BIGGER BREW → Staff Mode → Kiosk Settings → Receipt Printer**.
4. Refresh the paired Bluetooth printers.
5. Select the XP-58H.
6. The kiosk connects to it and stores its Bluetooth address/name.
7. Use **CHECK CONNECTION** to verify the saved device.

The Bluetooth plugin used by this stage reads already-paired printers. It does not perform a general Bluetooth discovery scan inside the app.

## Android

The `print_bluetooth_thermal` package supports Android and checks `BLUETOOTH_CONNECT` permission on Android 12+; it does not require location permission for this printer workflow. If the app reports that Nearby devices permission is missing, grant Bluetooth/Nearby devices permission to Bigger Brew in Android settings and refresh the printer list.

If the project later adds an explicit Android runtime-permission flow, keep the permissions limited to the Bluetooth printer use case.

## XP-58H

Use **58mm** paper for the XP-58H. The Kiosk Settings default has been changed to 58mm for the current printer hardware.

## Next step

Implement direct ESC/POS receipt generation and send the generated bytes through the saved XP-58H Bluetooth connection. Keep the current PDF/system-printer path as a fallback until physical XP-58H printing is validated.
