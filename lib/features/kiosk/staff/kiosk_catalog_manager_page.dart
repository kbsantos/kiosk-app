import 'package:bigger_brew_kiosk/features/catalog/catalog_manager_dashboard.dart';
import 'package:flutter/material.dart';

/// Adapter from the existing Staff Tools flow
/// into the unified K15.3 catalog manager.
class KioskCatalogManagerPage extends StatelessWidget {
  const KioskCatalogManagerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const CatalogManagerDashboardPage();
  }
}
